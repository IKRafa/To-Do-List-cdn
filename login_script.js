document.addEventListener("DOMContentLoaded",()=>{const tabs=document.querySelectorAll(".tab-btn");const boxes=document.querySelectorAll(".form-box");function activateTab(targetId){tabs.forEach((t)=>t.classList.remove("active"));boxes.forEach((b)=>b.classList.remove("active"));const targetBtn=document.querySelector(`.tab-btn[data-target="${targetId}"]`);const targetBox=document.getElementById(targetId);if(targetBtn&&targetBox){targetBtn.classList.add("active");targetBox.classList.add("active")}}
tabs.forEach((btn)=>{btn.addEventListener("click",()=>{const target=btn.getAttribute("data-target");activateTab(target)})});const hash=window.location.hash;if(hash==="#register-form"||hash==="#register-box"){activateTab("register-box")}else if(hash==="#login-form"||hash==="#login-box"){activateTab("login-box")}})
